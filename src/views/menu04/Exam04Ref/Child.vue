<!-- 컴포넌트 UI 정의 -->
<template>
  <v-card>
    <v-card-title>Child</v-card-title>
    <v-card-text>
      <p>data: {{ data }}</p>
      <v-divider class="mt-2 mb-2" />
      <v-btn @click="handleParentData" small class="mr-2 white--text" color="#2196F3"
        >부모 데이터 읽기</v-btn
      >
      <v-btn @click="handleParentMethod" small class="mr-2 white--text" color="#2196F3"
        >부모 메소드 호출</v-btn
      >
      <v-divider class="mt-2 mb-2" />
    </v-card-text>
  </v-card>
</template>

<script>
export default {
  // 컴포넌트의 대표이름(devtools에 나오는 이름)
  name: "Child",
  // 추가하고 싶은 컴포넌트 등록(import something from "/path")
  components: {},
  // 컴포넌트 데이터 정의
  data() {
    return {
      data: "child-data",
    };
  },
  // 컴포넌트 메소드 정의
  methods: {
    childMethod() {
      console.log("childMethod() 실행");
    },
    handleParentData() {
      console.log(this.$parent.data);
    },
    handleParentMethod() {
      this.$parent.parentMethod();
    },
  },
};
</script>

<!-- 컴포넌트 스타일 정의 -->
<style scoped>
/* scoped: 전역 범위X, 컴포넌트 범위 */
</style>